# Generate a list of decimal values from 1% to 100%
decimal_discounts = [i / 100 for i in range(1, 101)]

# Print the list of decimal discounts
print("Decimal values for discounts from 1% to 100%: ")
print(decimal_discounts)
